<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class="list text-center">
        <h1><%= @cocktail.name %></h1>
        <hr/>
        <div>
          <div>
            <%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill %>
          </div>
          <% @cocktail.doses.each do |dose| %>
          <div class = "cocktail">
            <%= dose.description %>
          </div>
          <div class = "cocktail">
            <p>Ingrédient : <%= dose.ingredient.name %></p>
          </div>
          <div class = "cocktail">
            <p><strong><%= link_to "delete", dose_path(dose), method: :delete %> </li></strong></p>
          </div>
          <% end %>
          <hr/>
        </div>
      </div>
    </div>
  </div>
